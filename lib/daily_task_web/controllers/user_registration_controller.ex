defmodule DailyTaskWeb.UserRegistrationController do
  use DailyTaskWeb, :controller

  alias DailyTask.Accounts
  alias DailyTask.Accounts.User
  alias DailyTaskWeb.UserAuth

  def new(conn, _params) do
    changeset = Accounts.change_user_registration(%User{})
    render(conn, :new, changeset: changeset)
  end

  def create(conn, %{"user" => user_params}) do
    case Accounts.register_user(user_params) do
      {:ok, user} ->
        conn
        |> put_flash(:info, "Bruker opprettet vellykket.")
        |> UserAuth.log_in_user(user)

      {:error, %Ecto.Changeset{} = changeset} ->
        render(conn, :new, changeset: changeset)
    end
  end
end
